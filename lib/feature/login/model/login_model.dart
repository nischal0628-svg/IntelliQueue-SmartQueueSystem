/*class LoginResponse {
  final String token;
  final String userId;
  final String phone;

  LoginResponse({
    required this.token,
    required this.userId,
    required this.phone,
  });

  factory LoginResponse.fromJson(Map<String, dynamic> json) {
    return LoginResponse(
      token: json['token'],
      userId: json['userId'],
      phone: json['phone'],
    );
  }
}
*/